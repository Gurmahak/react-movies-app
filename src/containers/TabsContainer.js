import React, { Component } from 'react'
import Movies from '../components/Tabs'

export default class MoviesContainer extends Component {
    render(){
    // const { search } = this.state
    // const { text } = search

    return(
        <Movies />    
    )   
    }
}